import React from "react";

import Markdown from "../components/Markdown";
const Write = () => {
    return (
        <div className="write">
            <Markdown />
        </div>
    );
};

export default Write;
